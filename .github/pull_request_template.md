**What this changes**

**Why**

**Checklist**

- [ ] `pnpm verify` passes
- [ ] `pnpm test` passes
- [ ] Added a changeset (`pnpm changeset`) if this should reach npm
- [ ] If it touches the `.iki` schema, said so in the changeset — before 1.0 a
      tightening can reject a model an earlier release accepted
